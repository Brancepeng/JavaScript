

Math对象是一个内置对象，期属性和方法有：

A.属性：

可以通过console.dir(Math)获取所有属性和方法。

下面其中几个比较重要但是容易混淆的方法：

Math.ceil(x);  //返回x向上取整后的值.

Math.floor(x);  //返回小于x的最大整数。

Math.random(x);返回0到1之间的伪随机数。

Math.round(x);  //返回四舍五入后的整数.

Math.trunc(x);   //返回x的整数部分,去除小数.